import 'package:flutter/material.dart';

class KitLoader extends StatelessWidget {
  const KitLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}
