import 'package:flutter/material.dart';

class CoreButton extends StatelessWidget {
  const CoreButton({super.key});

  @override
  Widget build(BuildContext context) {
    return ElevatedButton(onPressed: () {}, child: const Text('Button'));
  }
}
