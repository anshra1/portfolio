import 'package:flutter/material.dart';

class CoreLoader extends StatelessWidget {
  const CoreLoader({super.key});

  @override
  Widget build(BuildContext context) {
    return const CircularProgressIndicator();
  }
}
