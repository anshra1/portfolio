import 'package:flutter/material.dart';

/// Centralized definition of application seed colors.
/// These seeds are used by the theme generator to create the full color palette.
class AppColors {
  const AppColors._();

  static const primary = Color(0xFF1976D2); // Blue primary
  static const secondary = Color(0xFF625B71);
  static const tertiary = Color(0xFF7D5260);
  static const neutral = Color(0xFF5E5E5E);
  static const neutralVariant = Color(0xFF5E6366);
  static const error = Color(0xFFBA1A1A);
  static const success = Color(0xFF146B3A);
  static const warning = Color(0xFFF7931A);
  static const info = Color(0xFF0077B6);
}
