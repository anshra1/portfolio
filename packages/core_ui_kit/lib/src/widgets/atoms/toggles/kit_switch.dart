import 'package:flutter/material.dart';

class KitSwitch extends StatelessWidget {
  const KitSwitch({super.key});

  @override
  Widget build(BuildContext context) {
    return Switch(value: true, onChanged: (_) {});
  }
}
