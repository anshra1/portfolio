import 'package:flutter/material.dart';

class KitDialog extends StatelessWidget {
  const KitDialog({super.key});

  @override
  Widget build(BuildContext context) {
    return const Dialog(child: Text('Dialog'));
  }
}
